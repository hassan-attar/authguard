apiVersion: skaffold/v4beta11
kind: Config
manifests:
  rawYaml:
    - ./k8s/*
build:
  local:
    push: false
  artifacts:
    - image: hassanattar/accounts-service
      context: ..
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - dest: .
            src: "../src/**/*.ts"
